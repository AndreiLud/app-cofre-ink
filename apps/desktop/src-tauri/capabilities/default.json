{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "What the page is allowed to ask the shell for, which is almost nothing. The application does its work in the page and keeps its database there, so the only thing it needs from the shell is the window itself.",
	"windows": ["main"],
	"permissions": ["core:default"]
}
